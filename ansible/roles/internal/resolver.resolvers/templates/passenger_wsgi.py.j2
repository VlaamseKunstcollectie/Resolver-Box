#!/usr/bin/env python

import sys
import os

INTERP = os.path.expanduser("{{base_dir}}venv/bin/python")
if sys.executable != INTERP:
    os.execl(INTERP, INTERP, *sys.argv)

"""
sys.path.append(os.getcwd())
"""
sys.path.append('{{base_dir}}')

from resolver import app as application
